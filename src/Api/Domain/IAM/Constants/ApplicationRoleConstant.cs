namespace Todo.Api.Domain.IAM.Roles;

public static class ApplicationRoleConstant
{
    public static class Defaults
    {
        public const string Admin = nameof(Admin);
        public const string User = nameof(User);
    }
}